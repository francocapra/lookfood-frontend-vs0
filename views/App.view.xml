<mvc:View controllerName="lookfood.resources.Lookfood.controllers.App" 
    xmlns="sap.m" 
    xmlns:mvc="sap.ui.core.mvc" 
    displayBlock="true">
    <App id="appRoot"
        busy="{appView>/busy}"
		busyIndicatorDelay="{appView>/delay}">
    </App>
</mvc:View>